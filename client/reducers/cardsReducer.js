const initialState = {
  englishWord: 'English',
  chineseWord: '汉字',
  pinyin: 'Pīnyīn',
};

const cardsReducer = (state = initialState, action) => {
  return state;
};

export default cardsReducer;
